const card = document.querySelectorAll('.card');    //retorna todos los elementos que coincidan con ese selector
console.log(card);

const formularios = document.querySelectorAll('#formularios');
console.log(formularios);

const noExiste = document.querySelectorAll('.noExiste');
console.log(noExiste);