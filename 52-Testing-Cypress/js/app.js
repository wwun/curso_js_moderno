//cypress también usa npm, se inicia con npm init
//se instala cypress con npm i --save-dev cypress
//aquí se usa npx cypress open, esto abre la aplicación de escritorio y crea una carpeta cypress

//cypress ha cambiado mucho desde el video, es mejor aprenderlo desde otro lado, queda pendiente

import App from './classes/App.js';

const app = new App();